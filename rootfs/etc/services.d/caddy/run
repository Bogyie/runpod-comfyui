#!/usr/bin/env bash
set -euo pipefail

WORKSPACE_DIR="${WORKSPACE_DIR:-/workspace}"
mkdir -p "${WORKSPACE_DIR}/logs"

exec >> "${WORKSPACE_DIR}/logs/caddy.log" 2>&1
exec caddy run --config /etc/caddy/Caddyfile --adapter caddyfile
